---
/**
 * Mis Pagos - Portal Alumno
 * Historial de pagos y estado de cuenta
 */
import DashboardLayout from "../../layouts/DashboardLayout.astro";
import Card from "../../components/ui/Card.astro";
import Button from "../../components/ui/Button.astro";

const alumnoData = {
    nombre: "Juan Pérez García",
    rut: "18.765.432-1",
    curso: "Clase B",
    totalMatricula: 280000,
    totalPagado: 180000,
    saldoPendiente: 100000,
};

const pagos = [
    {
        id: 1,
        fecha: "2026-01-15",
        concepto: "Matrícula inicial",
        monto: 100000,
        metodo: "Transferencia",
        comprobante: "TRF-001.pdf",
    },
    {
        id: 2,
        fecha: "2026-01-28",
        concepto: "Cuota 1/3",
        monto: 50000,
        metodo: "Efectivo",
        comprobante: null,
    },
    {
        id: 3,
        fecha: "2026-02-03",
        concepto: "Cuota 2/3",
        monto: 30000,
        metodo: "Débito",
        comprobante: "DEB-002.pdf",
    },
];

const proximoPago = {
    fecha: "2026-02-20",
    monto: 100000,
    concepto: "Cuota 3/3 (saldo final)",
};
---

<DashboardLayout title="Mis Pagos" userRole="alumno">
    <div class="mb-8">
        <h1 class="text-3xl font-semibold text-gray-900 mb-2">Mis Pagos</h1>
        <p class="text-secondary">Estado de cuenta y historial de pagos</p>
    </div>

    <!-- Datos Alumno -->
    <Card
        class="mb-6 bg-gradient-to-r from-blue-50 to-blue-100 border-l-4 border-blue-500"
    >
        <div class="flex items-start justify-between">
            <div>
                <p class="text-sm text-blue-900 font-semibold mb-1">
                    👤 Alumno
                </p>
                <p class="text-lg font-bold text-blue-900">
                    {alumnoData.nombre}
                </p>
                <p class="text-sm text-blue-800">
                    RUT: {alumnoData.rut} • Curso: {alumnoData.curso}
                </p>
            </div>
        </div>
    </Card>

    <!-- Resumen Financiero -->
    <div class="grid sm:grid-cols-3 gap-4 mb-6">
        <Card class="border-l-4 border-gray-500">
            <p class="text-sm text-secondary mb-1">Total Matrícula</p>
            <p class="text-3xl font-bold text-gray-900">
                ${alumnoData.totalMatricula.toLocaleString("es-CL")}
            </p>
        </Card>

        <Card class="border-l-4 border-green-500">
            <p class="text-sm text-secondary mb-1">Total Pagado</p>
            <p class="text-3xl font-bold text-green-600">
                ${alumnoData.totalPagado.toLocaleString("es-CL")}
            </p>
            <p class="text-xs text-green-700 mt-1">
                {
                    Math.round(
                        (alumnoData.totalPagado / alumnoData.totalMatricula) *
                            100,
                    )
                }% completado
            </p>
        </Card>

        <Card class="border-l-4 border-red-500">
            <p class="text-sm text-secondary mb-1">Saldo Pendiente</p>
            <p class="text-3xl font-bold text-red-600">
                ${alumnoData.saldoPendiente.toLocaleString("es-CL")}
            </p>
        </Card>
    </div>

    <!-- Próximo Pago -->
    {
        alumnoData.saldoPendiente > 0 && (
            <Card class="mb-6 bg-yellow-50 border-l-4 border-yellow-500">
                <div class="flex items-start gap-3">
                    <svg
                        class="w-6 h-6 text-yellow-600 flex-shrink-0 mt-1"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                        />
                    </svg>
                    <div class="flex-1">
                        <p class="font-semibold text-yellow-900 mb-1">
                            📅 Próximo Pago Programado
                        </p>
                        <p class="text-sm text-yellow-800">
                            <span class="font-bold">{proximoPago.fecha}</span> •
                            <span class="font-bold">
                                ${proximoPago.monto.toLocaleString("es-CL")}
                            </span>{" "}
                            •{proximoPago.concepto}
                        </p>
                    </div>
                    <Button variant="primary" onclick="registrarPago()">
                        Pagar Ahora
                    </Button>
                </div>
            </Card>
        )
    }

    <!-- Historial de Pagos -->
    <Card>
        <div class="flex items-center justify-between mb-4">
            <h2 class="text-xl font-semibold text-gray-900">
                📊 Historial de Pagos
            </h2>
            <Button variant="secondary" onclick="descargarEstadoCuenta()">
                <svg
                    class="w-4 h-4 mr-2"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
                    ></path>
                </svg>
                Descargar Estado de Cuenta
            </Button>
        </div>

        <div class="overflow-x-auto">
            <table class="w-full">
                <thead>
                    <tr class="border-b border-gray-200">
                        <th
                            class="text-left py-3 px-4 text-xs font-semibold text-gray-700 uppercase"
                            >Fecha</th
                        >
                        <th
                            class="text-left py-3 px-4 text-xs font-semibold text-gray-700 uppercase"
                            >Concepto</th
                        >
                        <th
                            class="text-right py-3 px-4 text-xs font-semibold text-gray-700 uppercase"
                            >Monto</th
                        >
                        <th
                            class="text-left py-3 px-4 text-xs font-semibold text-gray-700 uppercase"
                            >Método</th
                        >
                        <th
                            class="text-center py-3 px-4 text-xs font-semibold text-gray-700 uppercase"
                            >Comprobante</th
                        >
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-100">
                    {
                        pagos.map((pago) => (
                            <tr class="hover:bg-gray-50 transition-colors">
                                <td class="py-3 px-4 text-sm text-gray-900">
                                    {pago.fecha}
                                </td>
                                <td class="py-3 px-4 text-sm text-gray-900">
                                    {pago.concepto}
                                </td>
                                <td class="py-3 px-4 text-sm text-right">
                                    <span class="font-bold text-green-600">
                                        ${pago.monto.toLocaleString("es-CL")}
                                    </span>
                                </td>
                                <td class="py-3 px-4">
                                    <span
                                        class={`inline-flex px-2 py-1 rounded-full text-xs font-medium ${
                                            pago.metodo === "Efectivo"
                                                ? "bg-green-100 text-green-800"
                                                : pago.metodo ===
                                                    "Transferencia"
                                                  ? "bg-blue-100 text-blue-800"
                                                  : "bg-purple-100 text-purple-800"
                                        }`}
                                    >
                                        {pago.metodo === "Efectivo"
                                            ? "💵"
                                            : pago.metodo === "Transferencia"
                                              ? "🏦"
                                              : "💳"}{" "}
                                        {pago.metodo}
                                    </span>
                                </td>
                                <td class="py-3 px-4 text-center">
                                    {pago.comprobante ? (
                                        <button
                                            class="text-blue-600 hover:text-blue-800 text-xs font-medium hover:underline"
                                            onclick={`verComprobante('${pago.comprobante}')`}
                                        >
                                            📄 Ver
                                        </button>
                                    ) : (
                                        <span class="text-xs text-gray-400">
                                            -
                                        </span>
                                    )}
                                </td>
                            </tr>
                        ))
                    }
                </tbody>
            </table>
        </div>

        <!-- Total Pagado Summary -->
        <div class="mt-4 pt-4 border-t border-gray-200 flex justify-end">
            <div class="text-right">
                <p class="text-sm text-secondary mb-1">
                    Total Pagado a la Fecha
                </p>
                <p class="text-2xl font-bold text-green-600">
                    ${alumnoData.totalPagado.toLocaleString("es-CL")}
                </p>
            </div>
        </div>
    </Card>

    <!-- Métodos de Pago Disponibles -->
    <Card class="mt-6">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">
            💳 Métodos de Pago Disponibles
        </h2>
        <div class="grid sm:grid-cols-3 gap-4">
            <div class="p-4 border border-gray-200 rounded-lg">
                <p class="font-semibold text-gray-900 mb-2">
                    🏦 Transferencia Bancaria
                </p>
                <p class="text-xs text-secondary mb-2">
                    Banco Estado • Cta. Corriente
                </p>
                <p class="text-xs font-mono text-gray-700">123-456789-01</p>
                <p class="text-xs text-gray-600 mt-2">RUT: 76.XXX.XXX-X</p>
            </div>

            <div class="p-4 border border-gray-200 rounded-lg">
                <p class="font-semibold text-gray-900 mb-2">💵 Efectivo</p>
                <p class="text-xs text-secondary mb-2">
                    En secretaría de la escuela
                </p>
                <p class="text-xs text-gray-700">Lun-Vie: 9:00 - 18:00</p>
                <p class="text-xs text-gray-700">Sáb: 9:00 - 13:00</p>
            </div>

            <div class="p-4 border border-gray-200 rounded-lg">
                <p class="font-semibold text-gray-900 mb-2">
                    💳 Débito/Crédito
                </p>
                <p class="text-xs text-secondary mb-2">
                    Terminal POS en secretaría
                </p>
                <p class="text-xs text-gray-700">Todas las tarjetas</p>
                <p class="text-xs text-gray-700">Hasta 3 cuotas sin interés</p>
            </div>
        </div>
    </Card>
</DashboardLayout>

<script>
    function registrarPago() {
        alert(`💳 Registrar Nuevo Pago

Monto pendiente: $100.000

Métodos disponibles:
• Transferencia bancaria
• Efectivo en secretaría
• Débito/Crédito (POS)

Contacta a secretaría:
📞 +56 9 8765 4321
📧 pagos@escuela.cl`);
    }

    function descargarEstadoCuenta() {
        alert(`📄 Generando Estado de Cuenta

Contenido:
• Datos alumno
• Total matrícula
• Pagos realizados
• Saldo pendiente
• Próximo pago programado

Formato: PDF
Descarga iniciada...`);
    }

    (window as any).verComprobante = function (archivo: string) {
        alert(`📄 Comprobante de Pago

Archivo: ${archivo}

Contenido:
• Fecha y hora pago
• Monto pagado
• Método de pago
• Número transacción
• Firma autorizada

Descarga iniciada...`);
    };
</script>
