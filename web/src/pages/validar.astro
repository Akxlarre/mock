---
/**
 * Validador de Certificados QR
 * RF-047: Validación de certificados con código QR
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import Card from '../components/ui/Card.astro';
import Button from '../components/ui/Button.astro';

// Query params
const codigo = Astro.url.searchParams.get('codigo');

// Simular BD de certificados
const certificados: Record<string, any> = {
  'CERT2026-001-ABC123': {
    codigo: 'CERT2026-001-ABC123',
    alumno: 'María González Pérez',
    rut: '18.765.432-1',
    curso: 'Clase B',
    fechaEmision: '2026-02-03',
    horasTeoricas: 18,
    horasPracticas: 12,
    instructor: 'Carlos Rojas',
    valido: true,
    estado: 'vigente'
  },
  'CERT2026-002-XYZ789': {
    codigo: 'CERT2026-002-XYZ789',
    alumno: 'Juan Pérez Soto',
    rut: '19.123.456-7',
    curso: 'Clase B',
    fechaEmision: '2026-01-20',
    horasTeoricas: 18,
    horasPracticas: 12,
    instructor: 'Ana Martínez',
    valido: false,
    estado: 'revocado',
    motivoRevocacion: 'Pago pendiente detectado posterior a emisión'
  }
};

const certificado = codigo ? certificados[codigo] : null;
---

<BaseLayout title="Validador de Certificados - Escuela">
  <div class="min-h-screen bg-linear-to-b from-gray-50 to-white py-12">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Header -->
      <div class="text-center mb-10">
        <div class="w-20 h-20 bg-blue-600 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
        </div>
        <h1 class="text-4xl font-bold text-gray-900 mb-3">
          Validador de Certificados
        </h1>
        <p class="text-lg text-secondary">
          Verifica la autenticidad de certificados emitidos
        </p>
      </div>

      {!codigo && (
        <!-- Formulario de Búsqueda -->
        <Card class="mb-8">
          <h2 class="text-xl font-semibold text-gray-900 mb-4">
            Ingresa el Código del Certificado
          </h2>
          <p class="text-sm text-secondary mb-4">
            El código se encuentra en la parte inferior del certificado o en el código QR
          </p>

          <form id="form-buscar" class="space-y-4">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">
                Código del Certificado
              </label>
              <input
                type="text"
                id="codigo-input"
                name="codigo"
                placeholder="CERT2026-001-ABC123"
                class="input input-bordered w-full min-h-11 text-lg font-mono"
                required
              />
              <p class="text-xs text-secondary mt-1">
                Ejemplo: CERT2026-001-ABC123
              </p>
            </div>

            <Button type="submit" variant="primary" class="w-full">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
              </svg>
              Validar Certificado
            </Button>
          </form>
        </Card>

        <!-- Instrucciones -->
        <Card class="bg-blue-50 border-blue-200">
          <div class="flex items-start gap-3">
            <svg class="w-6 h-6 text-blue-600 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <div>
              <p class="font-semibold text-blue-900 mb-2">¿Cómo usar este validador?</p>
              <ol class="text-sm text-blue-800 space-y-1 list-decimal list-inside">
                <li>Solicita al alumno que muestre su certificado físico o digital</li>
                <li>Escanea el código QR o ingresa el código manualmente</li>
                <li>Verifica que los datos mostrados coincidan con el documento</li>
                <li>Confirma el estado "VIGENTE" antes de aceptar el certificado</li>
              </ol>
            </div>
          </div>
        </Card>
      )}

      {codigo && !certificado && (
        <!-- Certificado No Encontrado -->
        <Card class="mb-6 border-l-4 border-red-500">
          <div class="flex items-start gap-4">
            <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center shrink-0">
              <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
              </svg>
            </div>
            <div class="flex-1">
              <h2 class="text-2xl font-bold text-red-900 mb-2">
                ❌ Certificado No Válido
              </h2>
              <p class="text-red-800 mb-3">
                El código ingresado no corresponde a ningún certificado emitido por nuestra escuela.
              </p>
              <div class="bg-red-50 border border-red-200 rounded p-3 mb-4">
                <p class="text-sm text-red-900 font-mono break-all">
                  Código buscado: <strong>{codigo}</strong>
                </p>
              </div>
              <p class="text-sm text-red-700">
                <strong>Posibles causas:</strong>
              </p>
              <ul class="text-sm text-red-700 list-disc list-inside mt-1">
                <li>El código fue ingresado incorrectamente</li>
                <li>El certificado es falso o ha sido alterado</li>
                <li>El certificado no pertenece a esta escuela</li>
              </ul>
            </div>
          </div>
        </Card>

        <div class="flex gap-3">
          <Button variant="secondary" href="/validar">
            Intentar Nuevo Código
          </Button>
          <Button variant="primary" href="mailto:seguridad@escuela.cl">
            Reportar Certificado Sospechoso
          </Button>
        </div>
      )}

      {certificado && certificado.valido && (
        <!-- Certificado Válido -->
        <Card class="mb-6 border-l-4 border-green-500">
          <div class="text-center py-6 mb-6 border-b border-gray-200">
            <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <h2 class="text-3xl font-bold text-green-600 mb-2">
              ✓ CERTIFICADO VÁLIDO
            </h2>
            <p class="text-lg text-gray-700">
              Este certificado fue emitido legítimamente por nuestra escuela
            </p>
            <div class="inline-flex mt-4 px-4 py-2 bg-green-100 text-green-800 rounded-full">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0121 12c0 .591-.047 1.172-.135 1.738m-2.747 8.226a12 12 0 01-16.236 0m2.747-8.226A11.959 11.959 0 013 12c0-2.39.69-4.62 1.882-6.5M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <span class="font-semibold">Estado: {certificado.estado.toUpperCase()}</span>
            </div>
          </div>

          <!-- Datos del Certificado -->
          <div class="space-y-4">
            <div class="grid sm:grid-cols-2 gap-4">
              <div>
                <p class="text-xs text-secondary uppercase tracking-wide mb-1">Código</p>
                <p class="text-sm font-mono font-semibold text-gray-900">{certificado.codigo}</p>
              </div>
              <div>
                <p class="text-xs text-secondary uppercase tracking-wide mb-1">Fecha Emisión</p>
                <p class="text-sm font-semibold text-gray-900">
                  {new Date(certificado.fechaEmision).toLocaleDateString('es-CL', { day: '2-digit', month: 'long', year: 'numeric' })}
                </p>
              </div>
            </div>

            <div class="border-t border-gray-200 pt-4">
              <h3 class="text-sm font-semibold text-gray-700 mb-3">Datos del Alumno</h3>
              <div class="grid sm:grid-cols-2 gap-4">
                <div>
                  <p class="text-xs text-secondary uppercase tracking-wide mb-1">Nombre Completo</p>
                  <p class="text-base font-bold text-gray-900">{certificado.alumno}</p>
                </div>
                <div>
                  <p class="text-xs text-secondary uppercase tracking-wide mb-1">RUT</p>
                  <p class="text-base font-bold text-gray-900">{certificado.rut}</p>
                </div>
              </div>
            </div>

            <div class="border-t border-gray-200 pt-4">
              <h3 class="text-sm font-semibold text-gray-700 mb-3">Detalles del Curso</h3>
              <div class="grid sm:grid-cols-3 gap-4">
                <div>
                  <p class="text-xs text-secondary uppercase tracking-wide mb-1">Tipo Licencia</p>
                  <p class="text-sm font-semibold text-gray-900">{certificado.curso}</p>
                </div>
                <div>
                  <p class="text-xs text-secondary uppercase tracking-wide mb-1">Horas Teóricas</p>
                  <p class="text-sm font-semibold text-gray-900">{certificado.horasTeoricas}h</p>
                </div>
                <div>
                  <p class="text-xs text-secondary uppercase tracking-wide mb-1">Horas Prácticas</p>
                  <p class="text-sm font-semibold text-gray-900">{certificado.horasPracticas}h</p>
                </div>
              </div>
            </div>

            <div class="border-t border-gray-200 pt-4">
              <p class="text-xs text-secondary uppercase tracking-wide mb-1">Instructor Principal</p>
              <p class="text-sm font-semibold text-gray-900">{certificado.instructor}</p>
            </div>
          </div>

          <!-- Sello Digital -->
          <div class="mt-6 p-4 bg-gray-50 rounded-lg border border-gray-200">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-xs text-secondary uppercase tracking-wide mb-1">Verificado Digitalmente</p>
                <p class="text-sm text-gray-700">
                  Sistema de Gestión Escuela de Conductores v2.0
                </p>
              </div>
              <svg class="w-12 h-12 text-green-600" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm-2 16l-4-4 1.41-1.41L10 14.17l6.59-6.59L18 9l-8 8z"/>
              </svg>
            </div>
          </div>
        </Card>

        <div class="flex gap-3">
          <Button variant="secondary" href="/validar">
            Validar Otro Certificado
          </Button>
          <Button variant="primary" onClick="window.print()">
            Imprimir Validación
          </Button>
        </div>
      )}

      {certificado && !certificado.valido && (
        <!-- Certificado Revocado -->
        <Card class="mb-6 border-l-4 border-yellow-500">
          <div class="flex items-start gap-4">
            <div class="w-16 h-16 bg-yellow-100 rounded-full flex items-center justify-center shrink-0">
              <svg class="w-8 h-8 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
              </svg>
            </div>
            <div class="flex-1">
              <h2 class="text-2xl font-bold text-yellow-900 mb-2">
                ⚠️ CERTIFICADO REVOCADO
              </h2>
              <p class="text-yellow-800 mb-3">
                Este certificado fue emitido originalmente pero ha sido revocado.
              </p>

              <div class="bg-yellow-50 border border-yellow-200 rounded p-4 mb-4">
                <p class="text-sm font-semibold text-yellow-900 mb-2">Información del Certificado:</p>
                <p class="text-sm text-yellow-800">Alumno: <strong>{certificado.alumno}</strong></p>
                <p class="text-sm text-yellow-800">RUT: <strong>{certificado.rut}</strong></p>
                <p class="text-sm text-yellow-800">Fecha Emisión: <strong>{certificado.fechaEmision}</strong></p>
                <p class="text-sm text-yellow-800 mt-2">Estado: <strong class="uppercase">{certificado.estado}</strong></p>
              </div>

              <div class="bg-red-50 border border-red-200 rounded p-3">
                <p class="text-sm font-semibold text-red-900 mb-1">Motivo de Revocación:</p>
                <p class="text-sm text-red-800">{certificado.motivoRevocacion}</p>
              </div>
            </div>
          </div>
        </Card>

        <Card class="bg-blue-50 border-blue-200 mb-6">
          <p class="text-sm text-blue-900">
            <strong>Importante:</strong> Para más información sobre este certificado revocado, 
            contacta directamente con nuestra oficina de secretaría.
          </p>
          <p class="text-sm text-blue-800 mt-2">
            📞 +56 9 8765 4321 • 📧 secretaria@escuela.cl
          </p>
        </Card>

        <div class="flex gap-3">
          <Button variant="secondary" href="/validar">
            Validar Otro Certificado
          </Button>
          <Button variant="primary" href="mailto:secretaria@escuela.cl">
            Contactar Secretaría
          </Button>
        </div>
      )}

      <!-- Info Footer -->
      <div class="mt-8 text-center text-sm text-secondary">
        <p>Este validador verifica certificados en tiempo real contra nuestra base de datos</p>
        <p class="mt-1">
          <a href="/terminos" class="text-blue-600 hover:underline">Términos y Condiciones</a> • 
          <a href="/contacto" class="text-blue-600 hover:underline ml-2">Contacto</a>
        </p>
      </div>
    </div>
  </div>
</BaseLayout>

<script>
  const form = document.getElementById('form-buscar');
  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    const input = document.getElementById('codigo-input') as HTMLInputElement;
    const codigo = input?.value.trim().toUpperCase();
    
    if (codigo) {
      window.location.href = `/validar?codigo=${encodeURIComponent(codigo)}`;
    }
  });
</script>

<style>
  @media print {
    button, form, .no-print {
      display: none;
    }
  }
</style>
