---
/**
 * Dashboard Secretaria - Vista principal
 * Matrículas, agenda, pagos.
 * Próximas Clases: muestra estado pendiente | en_curso | finalizada (ver docs/ESTRATEGIA-CLASE-EN-CURSO.md).
 */
import DashboardLayout from "../../layouts/DashboardLayout.astro";
import Card from "../../components/ui/Card.astro";
import Button from "../../components/ui/Button.astro";

/** Comparación HH:mm; devuelve estado del bloque respecto a la hora actual */
function estadoClase(
    horaInicio: string,
    horaFin: string,
    ahora: string,
): "pendiente" | "en_curso" | "finalizada" {
    if (ahora < horaInicio) return "pendiente";
    if (ahora >= horaFin) return "finalizada";
    return "en_curso";
}

// Demo: hora actual fija para que una clase aparezca "En curso". En producción usar new Date() o sesión.
const HORA_ACTUAL_DEMO = "14:15";

const proximasClasesHoy = [
    {
        horaInicio: "14:00",
        horaFin: "14:45",
        alumno: "María González",
        instructor: "Carlos Rojas",
        tipo: "práctica" as const,
    },
    {
        horaInicio: "15:00",
        horaFin: "16:00",
        promocion: "Enero 2026 - Grupo A",
        instructor: "Ana López",
        tipo: "teórica" as const,
    },
    {
        horaInicio: "16:30",
        horaFin: "17:15",
        alumno: "Juan Pérez",
        instructor: "Carlos Rojas",
        tipo: "práctica" as const,
    },
].map((clase) => ({
    ...clase,
    estado: estadoClase(clase.horaInicio, clase.horaFin, HORA_ACTUAL_DEMO),
}));
---

<DashboardLayout
    title="Dashboard"
    userRole="secretaria"
    userName="Patricia Secretaria"
>
    <!-- Header -->
    <div class="mb-8">
        <h1 class="text-3xl font-semibold text-gray-900">Dashboard</h1>
        <p class="text-secondary mt-1">Bienvenida de vuelta, Patricia</p>
    </div>

    <!-- KPIs -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <Card class="p-6">
            <div class="flex items-center justify-between mb-2">
                <p class="text-sm text-secondary">Clases de Hoy</p>
                <div
                    class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center"
                >
                    <svg
                        class="w-5 h-5 text-blue-600"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
                        ></path>
                    </svg>
                </div>
            </div>
            <p class="text-3xl font-bold text-gray-900">18</p>
            <p class="text-xs text-secondary mt-1">12 prácticas, 6 teóricas</p>
        </Card>

        <Card class="p-6">
            <div class="flex items-center justify-between mb-2">
                <p class="text-sm text-secondary">Matrículas Pendientes</p>
                <div
                    class="w-10 h-10 bg-orange-100 rounded-lg flex items-center justify-center"
                >
                    <svg
                        class="w-5 h-5 text-orange-600"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                        ></path>
                    </svg>
                </div>
            </div>
            <p class="text-3xl font-bold text-gray-900">7</p>
            <p class="text-xs text-secondary mt-1">Requieren documentos</p>
        </Card>

        <Card class="p-6">
            <div class="flex items-center justify-between mb-2">
                <p class="text-sm text-secondary">Pagos Hoy</p>
                <div
                    class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center"
                >
                    <svg
                        class="w-5 h-5 text-green-600"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                        ></path>
                    </svg>
                </div>
            </div>
            <p class="text-3xl font-bold text-gray-900">$450K</p>
            <p class="text-xs text-green-600 mt-1">5 pagos recibidos</p>
        </Card>
    </div>

    <!-- ═══════════════════════════════════════════════════════ -->
    <!-- ALERTA DE MOROSIDAD (RF-039) -->
    <!-- ═══════════════════════════════════════════════════════ -->
    <Card
        class="mb-8 border-2 border-red-200 bg-gradient-to-r from-red-50 to-orange-50"
    >
        <div class="flex items-center justify-between mb-4">
            <div class="flex items-center gap-3">
                <div
                    class="w-10 h-10 bg-red-100 rounded-full flex items-center justify-center"
                >
                    <svg
                        class="w-5 h-5 text-red-600"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z"
                        ></path>
                    </svg>
                </div>
                <div>
                    <h2 class="text-lg font-semibold text-red-900">
                        ⚠️ Alerta de Morosidad
                    </h2>
                    <p class="text-sm text-red-700">
                        RF-039 · 4 alumnos con pagos vencidos
                    </p>
                </div>
            </div>
            <Button
                href="/secretaria/pagos"
                variant="secondary"
                class="text-sm border-red-300 text-red-800 hover:bg-red-100"
            >
                Ver todos →
            </Button>
        </div>

        <div class="space-y-2">
            {
                [
                    {
                        nombre: "Pedro Sánchez Díaz",
                        diasAtraso: 32,
                        monto: 180000,
                        ultimoPago: "2026-01-09",
                    },
                    {
                        nombre: "Roberto Fuentes M.",
                        diasAtraso: 21,
                        monto: 100000,
                        ultimoPago: "2026-01-20",
                    },
                    {
                        nombre: "Camila Vega Torres",
                        diasAtraso: 12,
                        monto: 50000,
                        ultimoPago: "2026-01-29",
                    },
                    {
                        nombre: "Francisco Mora L.",
                        diasAtraso: 7,
                        monto: 80000,
                        ultimoPago: "2026-02-03",
                    },
                ].map((alumno) => {
                    const esGrave = alumno.diasAtraso > 15;
                    return (
                        <div
                            class={`flex items-center justify-between p-3 rounded-lg border ${esGrave ? "bg-red-50 border-red-200" : "bg-yellow-50 border-yellow-200"}`}
                        >
                            <div class="flex items-center gap-3">
                                <span
                                    class="text-lg"
                                    title={
                                        esGrave
                                            ? "Atraso grave (>15 días)"
                                            : "Atraso leve (1-15 días)"
                                    }
                                >
                                    {esGrave ? "🔴" : "🟡"}
                                </span>
                                <div>
                                    <p class="text-sm font-medium text-gray-900">
                                        {alumno.nombre}
                                    </p>
                                    <p class="text-xs text-gray-500">
                                        Último pago: {alumno.ultimoPago}
                                    </p>
                                </div>
                            </div>
                            <div class="flex items-center gap-4">
                                <div class="text-right">
                                    <p
                                        class={`text-sm font-bold ${esGrave ? "text-red-700" : "text-yellow-700"}`}
                                    >
                                        ${alumno.monto.toLocaleString("es-CL")}
                                    </p>
                                    <p
                                        class={`text-xs font-medium ${esGrave ? "text-red-600" : "text-yellow-600"}`}
                                    >
                                        {alumno.diasAtraso} días de atraso
                                    </p>
                                </div>
                                <button
                                    class="p-1.5 text-gray-400 hover:text-blue-600 hover:bg-blue-50 rounded transition-colors"
                                    title="Contactar alumno"
                                    onclick={`alert('📞 Contactando a ${alumno.nombre}\\n\\nDeuda: $${alumno.monto.toLocaleString("es-CL")}\\nDías de atraso: ${alumno.diasAtraso}\\n\\nAcciones disponibles:\\n• Enviar recordatorio por email\\n• Llamar al alumno\\n• Registrar compromiso de pago')`}
                                >
                                    <svg
                                        class="w-4 h-4"
                                        fill="none"
                                        stroke="currentColor"
                                        viewBox="0 0 24 24"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
                                        />
                                    </svg>
                                </button>
                            </div>
                        </div>
                    );
                })
            }
        </div>

        <div
            class="mt-3 pt-3 border-t border-red-200 flex items-center justify-between"
        >
            <p class="text-xs text-red-700">
                <span class="font-semibold">Total en morosidad:</span> $410.000
            </p>
            <div class="flex items-center gap-4 text-xs text-gray-600">
                <span class="flex items-center gap-1">🔴 &gt;15 días</span>
                <span class="flex items-center gap-1">🟡 1-15 días</span>
            </div>
        </div>
    </Card>

    <!-- Main grid -->
    <div class="grid lg:grid-cols-2 gap-6">
        <!-- Tareas pendientes -->
        <Card>
            <div class="flex items-center justify-between mb-4">
                <h2 class="text-lg font-semibold text-gray-900">
                    Tareas Pendientes
                </h2>
                <Button
                    href="/tareas/crear"
                    variant="secondary"
                    class="text-sm"
                >
                    <svg
                        class="w-4 h-4 mr-1.5"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 4v16m8-8H4"></path>
                    </svg>
                    Crear tarea
                </Button>
            </div>
            <div class="space-y-3">
                {
                    [
                        {
                            titulo: "Validar documentos - María González",
                            urgente: true,
                            designadaPorAdmin: true,
                        },
                        {
                            titulo: "Confirmar clase 15:00 - Juan Pérez",
                            urgente: false,
                            designadaPorAdmin: true,
                        },
                        {
                            titulo: "Registrar pago mensualidad - Ana Martínez",
                            urgente: false,
                            designadaPorAdmin: false,
                        },
                        {
                            titulo: "Actualizar datos SENCE - Pedro Silva",
                            urgente: true,
                            designadaPorAdmin: true,
                        },
                    ].map((tarea) => (
                        <label class="flex items-start gap-3 p-3 bg-gray-50 rounded-lg cursor-pointer hover:bg-gray-100 transition-colors">
                            <input
                                type="checkbox"
                                class="mt-1 w-5 h-5 rounded border-gray-300 text-gray-900"
                            />
                            <div class="flex-1 min-w-0">
                                <p
                                    class={`text-sm ${tarea.urgente ? "font-semibold text-gray-900" : "text-gray-700"}`}
                                >
                                    {tarea.titulo}
                                </p>
                                <div class="flex flex-wrap items-center gap-2 mt-1.5">
                                    {tarea.urgente && (
                                        <span class="inline-block px-2 py-0.5 bg-red-100 text-red-800 text-xs font-medium rounded">
                                            Urgente
                                        </span>
                                    )}
                                    {tarea.designadaPorAdmin && (
                                        <span
                                            class="inline-flex items-center gap-1 px-2 py-0.5 bg-gray-200 text-gray-700 text-xs font-medium rounded"
                                            title="Asignada por el administrador"
                                        >
                                            <svg
                                                class="w-3.5 h-3.5"
                                                fill="none"
                                                stroke="currentColor"
                                                viewBox="0 0 24 24"
                                            >
                                                <path
                                                    stroke-linecap="round"
                                                    stroke-linejoin="round"
                                                    stroke-width="2"
                                                    d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"
                                                />
                                            </svg>
                                            Designada por administrador
                                        </span>
                                    )}
                                </div>
                            </div>
                        </label>
                    ))
                }
            </div>
        </Card>

        <!-- Próximas clases (hoy) con estado "en curso" -->
        <Card>
            <h2 class="text-lg font-semibold text-gray-900 mb-4">
                Próximas Clases (Hoy)
            </h2>
            <div class="space-y-3" role="list" aria-label="Clases del día">
                {
                    proximasClasesHoy.map((clase) => {
                        const enCurso = clase.estado === "en_curso";
                        const finalizada = clase.estado === "finalizada";
                        const label =
                            clase.estado === "en_curso"
                                ? `Clase en curso: ${clase.alumno || clase.promocion}`
                                : undefined;
                        return (
                            <div
                                role="listitem"
                                aria-current={enCurso ? "true" : undefined}
                                aria-label={label}
                                class={`flex items-center gap-3 p-3 border rounded-lg transition-colors ${
                                    enCurso
                                        ? "border-l-4 border-l-blue-500 border-gray-200 bg-blue-50/60"
                                        : finalizada
                                          ? "border-gray-200 bg-gray-50/50 opacity-90"
                                          : "border-gray-200"
                                }`}
                            >
                                <div
                                    class={`w-12 h-12 rounded-lg flex items-center justify-center font-bold text-sm shrink-0 ${
                                        enCurso
                                            ? "bg-blue-600 text-white"
                                            : "bg-gray-900 text-white"
                                    }`}
                                >
                                    {clase.horaInicio}
                                </div>
                                <div class="flex-1 min-w-0">
                                    <div class="flex flex-wrap items-center gap-2">
                                        <p class="text-sm font-medium text-gray-900 truncate">
                                            {clase.alumno || clase.promocion}
                                        </p>
                                        {enCurso && (
                                            <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800 border border-blue-200">
                                                <span
                                                    class="w-1.5 h-1.5 rounded-full bg-blue-500 animate-pulse"
                                                    aria-hidden="true"
                                                />
                                                En curso
                                            </span>
                                        )}
                                        {finalizada && (
                                            <span class="text-xs text-gray-500 font-medium">
                                                Finalizada
                                            </span>
                                        )}
                                    </div>
                                    <p class="text-xs text-secondary truncate mt-0.5">
                                        Instructor: {clase.instructor}
                                    </p>
                                </div>
                                <span
                                    class={`text-xs px-2 py-1 rounded-full font-medium shrink-0 ${
                                        clase.tipo === "práctica"
                                            ? "bg-green-100 text-green-800"
                                            : "bg-blue-100 text-blue-800"
                                    }`}
                                    title={
                                        clase.tipo === "práctica"
                                            ? "Clase práctica"
                                            : "Clase teórica"
                                    }
                                >
                                    {clase.tipo === "práctica" ? "🚗" : "📚"}
                                </span>
                            </div>
                        );
                    })
                }
            </div>
        </Card>
    </div>

    <!-- Quick actions -->
    <div class="mt-6">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">
            Acciones Rápidas
        </h2>
        <div class="grid md:grid-cols-3 gap-4">
            <Button
                href="/matricula/nuevo"
                variant="primary"
                class="justify-center"
            >
                <svg
                    class="w-5 h-5 mr-2"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"
                    ></path>
                </svg>
                Nueva Matrícula
            </Button>
            <Button href="/pagos" variant="secondary" class="justify-center">
                <svg
                    class="w-5 h-5 mr-2"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                    ></path>
                </svg>
                Registrar Pago
            </Button>
            <Button href="/agenda" variant="secondary" class="justify-center">
                <svg
                    class="w-5 h-5 mr-2"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
                    ></path>
                </svg>
                Ver Agenda
            </Button>
        </div>
    </div>
</DashboardLayout>

<style>
    input[type="checkbox"] {
        appearance: none;
        border: 1px solid rgb(209, 213, 219);
        border-radius: 0.25rem;
        cursor: pointer;
        position: relative;
    }

    input[type="checkbox"]:checked {
        background-color: rgb(17, 24, 39);
        border-color: rgb(17, 24, 39);
    }

    input[type="checkbox"]:checked::before {
        content: "";
        position: absolute;
        inset: 0;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 16 16' fill='white' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M12.207 4.793a1 1 0 010 1.414l-5 5a1 1 0 01-1.414 0l-2-2a1 1 0 011.414-1.414L6.5 9.086l4.293-4.293a1 1 0 011.414 0z'/%3E%3C/svg%3E");
        background-size: 100% 100%;
    }
</style>
