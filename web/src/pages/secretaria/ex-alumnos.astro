---
/**
 * Gestión de Ex-Alumnos y Reportes
 * RF-101 (Reportes Aprobación) y RF-102 (Encuestas)
 */
import DashboardLayout from "../../layouts/DashboardLayout.astro";
import Card from "../../components/ui/Card.astro";
import Button from "../../components/ui/Button.astro";

const comments = [
  { name: "Carlos González", stars: 5, text: "Excelente instructor, muy paciente. Me ayudó mucho con los estacionamientos." },
  { name: "Ana Rojas", stars: 4, text: "Buena experiencia general, aunque la plataforma online a veces es lenta." },
  { name: "Pedro Soto", stars: 5, text: "Muy recomendado. Aprobé mi examen a la primera gracias a las clases prácticas." },
  { name: "María Pérez", stars: 5, text: "Todo super bien organizado. La secretaria muy amable." },
];
---

<DashboardLayout title="Gestión de Ex-Alumnos y Reportes" userRole="secretaria">
  <div class="mb-8">
    <div class="flex items-center gap-2 text-sm text-secondary mb-2">
      <a href="/secretaria/dashboard" class="hover:text-gray-900">Inicio</a>
      <span>/</span>
      <a href="/secretaria/alumnos" class="hover:text-gray-900">Alumnos</a>
      <span>/</span>
      <span class="text-gray-900 font-medium">Ex-Alumnos</span>
    </div>

    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-3xl font-semibold text-gray-900">Gestión de Ex-Alumnos</h1>
        <p class="text-secondary mt-1">Reportes históricos y feedback</p>
      </div>
      <Button href="/secretaria/alumnos" variant="secondary">
        ← Volver a Alumnos Activos
      </Button>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
    <!-- Sección A: Reporte de Aprobación (RF-101) -->
    <div class="space-y-6">
      <h2 class="text-xl font-semibold text-gray-900">📊 Tasas de Aprobación</h2>
      
      <Card class="p-6">
        <div class="mb-6">
          <div class="flex justify-between items-end mb-2">
            <h3 class="font-medium text-gray-900">Aprobación Municipal</h3>
            <span class="text-2xl font-bold text-green-600">85%</span>
          </div>
          <div class="w-full bg-gray-200 rounded-full h-4 overflow-hidden">
            <div class="bg-green-500 h-4 rounded-full" style="width: 85%"></div>
          </div>
          <p class="text-xs text-secondary mt-2">Basado en 340 exámenes presentados este año</p>
        </div>

        <div>
           <div class="flex justify-between items-end mb-2">
            <h3 class="font-medium text-gray-900">Aprobación Examen Psicotécnico</h3>
            <span class="text-2xl font-bold text-blue-600">92%</span>
          </div>
          <div class="w-full bg-gray-200 rounded-full h-4 overflow-hidden">
            <div class="bg-blue-500 h-4 rounded-full" style="width: 92%"></div>
          </div>
           <p class="text-xs text-secondary mt-2">Alta tasa de éxito en primera instancia</p>
        </div>
      </Card>

      <Card class="p-6 bg-gradient-to-br from-indigo-50 to-white border border-indigo-100">
        <h3 class="font-semibold text-indigo-900 mb-2">Resumen Anual</h3>
        <div class="grid grid-cols-2 gap-4 text-center">
            <div class="bg-white p-3 rounded-lg shadow-sm">
                <p class="text-sm text-gray-500">Total Egresados</p>
                <p class="text-2xl font-bold text-gray-900">450</p>
            </div>
             <div class="bg-white p-3 rounded-lg shadow-sm">
                <p class="text-sm text-gray-500">Licencias Obtenidas</p>
                <p class="text-2xl font-bold text-green-600">412</p>
            </div>
        </div>
      </Card>
    </div>

    <!-- Sección B: Encuestas de Satisfacción (RF-102) -->
    <div class="space-y-6">
        <div class="flex items-center justify-between">
            <h2 class="text-xl font-semibold text-gray-900">⭐ Últimos Comentarios</h2>
            <div class="flex items-center gap-1 bg-yellow-50 px-3 py-1 rounded-full border border-yellow-200">
                <span class="text-yellow-500 font-bold">4.8</span>
                <span class="text-xs text-yellow-700">Promedio General</span>
            </div>
        </div>

        <div class="space-y-4">
            {comments.map((comment) => (
                <Card class="p-4">
                    <div class="flex justify-between items-start mb-2">
                        <div class="flex items-center gap-2">
                            <div class="w-8 h-8 rounded-full bg-gray-200 flex items-center justify-center text-xs font-bold text-gray-600">
                                {comment.name.split(" ").map(n => n[0]).join("")}
                            </div>
                            <span class="font-medium text-gray-900">{comment.name}</span>
                        </div>
                        <div class="flex text-yellow-400 text-sm">
                            {[...Array(5)].map((_, i) => (
                                <span>{i < comment.stars ? "★" : "☆"}</span>
                            ))}
                        </div>
                    </div>
                    <p class="text-gray-600 text-sm ml-10">"{comment.text}"</p>
                </Card>
            ))}
        </div>
        
        <Button variant="secondary" class="w-full justify-center">Ver todas las encuestas</Button>
    </div>
  </div>
</DashboardLayout>
