---
/**
 * Componente: Selector de Siguiente Clase Individual
 * Permite al alumno agendar su próxima clase en secuencia
 * RF-014: Agendamiento individual clase por clase
 */

export interface Props {
    nextClassNumber: number;
    totalClasses: number;
    canBook: boolean;
    pendingClassesCount: number;
}

const { nextClassNumber, totalClasses, canBook, pendingClassesCount } =
    Astro.props;
---

<div class="space-y-4">
    <div class="mb-4">
        <h3 class="text-lg font-semibold text-gray-900 mb-2">
            Agendamiento de Clases
        </h3>
        <p class="text-sm text-gray-600">
            Sistema de reserva individual - Agenda tus clases una por una según
            tu disponibilidad
        </p>
    </div>

    {
        nextClassNumber > totalClasses ? (
            <div class="p-6 bg-green-50 border border-green-200 rounded-lg">
                <div class="flex gap-3">
                    <svg
                        class="w-6 h-6 text-green-600 flex-shrink-0"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
                        />
                    </svg>
                    <div>
                        <p class="text-green-900 font-medium">
                            🎉 ¡Curso Completo!
                        </p>
                        <p class="text-sm text-green-800 mt-1">
                            Has completado las {totalClasses} clases prácticas
                            del curso Clase B.
                        </p>
                    </div>
                </div>
            </div>
        ) : canBook ? (
            <div class="p-6 bg-white border-2 border-blue-500 rounded-lg">
                <div class="flex items-start justify-between mb-4">
                    <div>
                        <p class="text-lg font-semibold text-gray-900">
                            Clase {nextClassNumber} de {totalClasses}
                        </p>
                        <p class="text-sm text-gray-600 mt-1">
                            Próxima clase disponible para agendar
                        </p>
                    </div>
                    <div class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-xs font-medium">
                        Disponible
                    </div>
                </div>

                <div class="space-y-3 mb-4">
                    <div class="p-3 bg-gray-50 rounded-lg">
                        <p class="text-xs font-medium text-gray-700 mb-2">
                            📋 Requisitos para agendar:
                        </p>
                        <ul class="text-xs text-gray-600 space-y-1">
                            <li class="flex items-start gap-2">
                                <span class="text-green-600 mt-0.5">✓</span>
                                <span>
                                    Completaste la clase{" "}
                                    {nextClassNumber - 1 > 0
                                        ? nextClassNumber - 1
                                        : "inicial"}
                                </span>
                            </li>
                            <li class="flex items-start gap-2">
                                <span class="text-green-600 mt-0.5">✓</span>
                                <span>
                                    Tienes {3 - pendingClassesCount} cupos
                                    disponibles para agendar
                                </span>
                            </li>
                            <li class="flex items-start gap-2">
                                <span class="text-blue-600 mt-0.5">ℹ️</span>
                                <span>
                                    Debes agendar con mínimo 24 horas de
                                    anticipación
                                </span>
                            </li>
                        </ul>
                    </div>
                </div>

                <a
                    href="/alumno/agendar"
                    class="block w-full px-4 py-3 bg-blue-600 text-white text-center font-medium rounded-lg hover:bg-blue-700 transition-colors"
                >
                    Agendar Clase {nextClassNumber}
                </a>
            </div>
        ) : (
            <div class="p-6 bg-orange-50 border border-orange-200 rounded-lg">
                <div class="flex gap-3">
                    <svg
                        class="w-6 h-6 text-orange-600 flex-shrink-0"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"
                        />
                    </svg>
                    <div>
                        <p class="text-orange-900 font-medium">
                            No puedes agendar más clases en este momento
                        </p>
                        <p class="text-sm text-orange-800 mt-1">
                            Ya tienes 3 clases agendadas (máximo permitido).
                            Completa o cancela una clase antes de agendar otra.
                        </p>
                    </div>
                </div>
            </div>
        )
    }

    <!-- Información del Sistema -->
    <div class="p-4 bg-blue-50 border border-blue-200 rounded-lg">
        <div class="flex gap-3">
            <svg
                class="w-5 h-5 text-blue-600 flex-shrink-0 mt-0.5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
            >
                <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                ></path>
            </svg>
            <div class="text-sm text-blue-800">
                <p class="font-medium mb-1">¿Cómo funciona el agendamiento?</p>
                <p>
                    A diferencia de un horario fijo, puedes agendar cada clase
                    cuando te acomode. Selecciona el día, hora e instructor
                    disponible para cada sesión. Debes completar las 12 clases
                    en orden secuencial (1→2→3...→12).
                </p>
            </div>
        </div>
    </div>
</div>
